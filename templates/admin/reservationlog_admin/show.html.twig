{% extends 'base.html.twig' %}

{% block title %}Reservationlog{% endblock %}

{% block body %}
    <h1>Reservationlog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idreslog</th>
                <td>{{ reservationlog.idreslog }}</td>
            </tr>
            <tr>
                <th>Idlog</th>
                <td>{{ reservationlog.idlog }}</td>
            </tr>
            <tr>
                <th>Idc</th>
                <td>{{ reservationlog.idc }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ reservationlog.dateDebut ? reservationlog.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ reservationlog.dateFin ? reservationlog.dateFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ reservationlog.montant }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reservationlog.status }}</td>
            </tr>
            <tr>
                <th>Modalites</th>
                <td>{{ reservationlog.modalites }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_reservationlog_admin_edit',

    {{ include('reservationlog_admin/_delete_form.html.twig') }}
{% endblock %}
