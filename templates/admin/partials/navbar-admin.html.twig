<nav class="navbar navbar-dark bg-horozia-dark shadow-sm px-4 py-3">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center gap-2 mb-0" href="{{ path('app_admin') }}">
            <img src="{{ asset('images/logo.png') }}" alt="Logo Horozia" width="58" height="58" style="object-fit: contain;">
        </a>

        <div class="d-flex align-items-center gap-3 text-white">
            <span><i class="bi bi-person-circle me-1"></i> Administrateur</span>
            <a href="#" class="btn btn-outline-light btn-sm">Déconnexion</a>
        </div>
    </div>
</nav>